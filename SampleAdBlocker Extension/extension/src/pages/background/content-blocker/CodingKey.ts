/**
 * Pairs matching properties of Trigger class and ContentBlocker trigger properties
 */
export enum CodingKey {
    ifDomain = 'if-domain',
    urlFilter = 'url-filter',
    unlessDomain = 'unless-domain',
    shortcut = 'url-shortcut',
}
